/* Main 페이지 */
import React from 'react';
import Header from '../components/Header';

const Main = () => {
    return (
        <>
            <Header isLogoActive={true} />
        </>
    );
};

export default Main;
